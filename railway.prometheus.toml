# Configuración Railway — servicio Prometheus (monitoreo, red privada)

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.prometheus"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
