# Configuración Railway — servicio Laravel (admin / nutriólogo)

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.laravel"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
