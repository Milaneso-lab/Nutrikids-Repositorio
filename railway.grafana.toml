# Configuración Railway — servicio Grafana (dashboards de monitoreo, expuesto públicamente)

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.grafana"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
